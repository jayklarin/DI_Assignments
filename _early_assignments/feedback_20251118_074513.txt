URL: https://github.com/jayklarin/DI_Assignments/blob/main/_early_assignments/dc_w01_d4_.py
Brief justification:
- correctness: The code accurately implements all the required functions (show_menu, add_item, update_price, delete_item, show_options, run_coffee_shop) as described in the problem statement. It correctly handles user input, updates the menu dictionary, and provides the specified output messages. It also starts with the specified initial data, which is essential for consistency with the tests.
- readability: The code is well-structured and easy to understand. The functions are clearly defined with docstrings explaining their purpose. Variable names are descriptive, and the code is consistently formatted.
- performance: The code's performance is efficient for the given task. The operations on the dictionary (adding, updating, deleting) have average time complexity of O(1). The show_menu function iterates through the dictionary once, which is also efficient.
- security: The code handles user input safely by using `strip()` to remove leading/trailing whitespace and `lower()` to convert input to lowercase, preventing potential issues with case sensitivity. It also includes error handling for invalid price inputs using a `try-except` block, preventing the program from crashing due to `ValueError`.

